import React from "react";
import { render } from "@testing-library/react";
import <%= name %> from "../src/<%= name %>";

describe("@osc/<%= name %>", () => {
  it("renders the component", () => {
    // Please write your tests
    expect(false).toBe(true);
  });
});